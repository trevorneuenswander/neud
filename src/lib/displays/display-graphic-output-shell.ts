/** Graphic output surfaces (Local URL, fullscreen, OBS) — never management checkerboard. */
export const DISPLAY_GRAPHIC_OUTPUT_BACKGROUND = "transparent" as const;

/** Electron BrowserWindow backing for transparent display HTML (ARGB). */
export const ELECTRON_TRANSPARENT_WINDOW_BACKGROUND = "#00000000" as const;
