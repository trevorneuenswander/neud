import { ProjectPlaceholderPage } from "@/components/projects/ProjectPlaceholderPage";

export default function ProjectDisplaysPage() {
  return (
    <ProjectPlaceholderPage
      title="Displays"
      description="OBS-ready display URLs will be configured in a future phase."
    />
  );
}
