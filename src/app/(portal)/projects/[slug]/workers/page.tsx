import { ProjectPlaceholderPage } from "@/components/projects/ProjectPlaceholderPage";

export default function ProjectWorkersPage() {
  return (
    <ProjectPlaceholderPage
      title="Workers"
      description="Background workers and data sources will be configured in a future phase."
    />
  );
}
