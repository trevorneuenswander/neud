import { ProjectPlaceholderPage } from "@/components/projects/ProjectPlaceholderPage";

export default function ProjectControllersPage() {
  return (
    <ProjectPlaceholderPage
      title="Controllers"
      description="Project-specific web controllers will be configured in a future phase."
    />
  );
}
