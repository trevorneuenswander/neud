# BAG-Graphics Worker

The BAG-Graphics background worker runs as a separate Node.js process.

It uses Puppeteer to continuously collect auction data and is not deployed to Vercel Functions.

Source code lives in `src/`.
